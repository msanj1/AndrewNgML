%A = [1,2,3,4;5,6,7,8;1,5,6,8];


%B = ones(5,1);

%C = zeros(2,1)

A= [0,1;1,1;1,2;1,3];




%X = [ones(m, 1), data(:,1)]; % Add a column of ones to x
%theta = zeros(2, 1); % initialize fitting parameters

% Some gradient descent settings
%iterations = 1500;
%alpha = 0.01;

